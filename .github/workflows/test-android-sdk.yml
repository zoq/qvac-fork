name: QVAC Android Tests (Device Farm)
# Reusable workflow for running Android tests via AWS Device Farm
# Originally from qvac-test-suite

on:
  workflow_call:
    inputs:
      working-directory:
        description: "Working directory containing qvac-test.config.js"
        required: false
        type: string
        default: "."
      project-directory:
        description: "Root project directory (where package.json lives)"
        required: true
        type: string
      consumer-timeout:
        description: "Timeout in seconds for consumer connection (mobile needs longer)"
        required: false
        type: number
        default: 600
      filter:
        description: "Filter tests by category or testId prefix (comma-separated)"
        required: false
        type: string
      suite:
        description: "Suite filter (e.g. 'smoke'). Empty = all tests."
        required: false
        type: string
      exclude-suite:
        description: "Exclude suites (comma-separated)"
        required: false
        type: string
      device-farm-timeout:
        description: "Maximum time in minutes to keep Device Farm session alive."
        required: false
        type: number
        default: 60
      device-farm-start-timeout:
        description: "Maximum time in minutes to wait for Device Farm runs to enter RUNNING before starting the producer."
        required: false
        type: number
        default: 90
      device-farm-project-arn:
        description: "AWS Device Farm project ARN"
        required: true
        type: string
      device-pools:
        description: 'JSON array of device pools [{name, arn}]'
        required: true
        type: string
      device-pool-name:
        description: "Explicit device pool name to run. When set, that pool is used as-is; when empty, a random AWS-available pool from device-pools is chosen."
        required: false
        type: string
        default: ""
      test-version:
        description: "Git ref to checkout (branch, tag, or SHA). Defaults to trigger ref."
        required: false
        type: string
      plugins:
        description: >-
          Optional comma-separated SDK plugin specifiers (e.g.
          "@qvac/sdk/tts-ggml/plugin,@qvac/sdk/llamacpp-completion/plugin").
          When set, a qvac.config.json is written into working-directory before
          the consumer build so the mobile bundle includes ONLY this subset.
          Used by the ggml multi-addon co-load smoke; empty = full plugin set.
        required: false
        type: string
        default: ""
      overlay-prebuild-addon:
        description: >-
          Optional addon short name (e.g. "tts-ggml"). When set, this run's
          `prebuilds` artifact is downloaded and its android-arm64 build is
          overlaid onto the installed @qvac/<addon> package before the consumer
          APK is bundled, so the co-load exercises THIS PR's freshly-built addon
          instead of the published npm baseline. Empty = published baseline
          (unchanged; SDK e2e never sets this).
        required: false
        type: string
        default: ""
    secrets:
      # MQTT_* and NPM_TOKEN are typically provided via the release environment
      # (see jobs with `environment: release`). They are declared required: false
      # here so `secrets: inherit` from a caller with no repo-level values does
      # not fail at the workflow_call boundary; the jobs that consume them have
      # environment access. AWS auth uses OIDC, so AWS_ACCESS_KEY_ID /
      # AWS_SECRET_ACCESS_KEY are not declared.
      # PAT_TOKEN and AWS_OIDC_ROLE_ARN remain required:true because they are
      # repo-level.
      PAT_TOKEN:
        required: true
      NPM_TOKEN:
        required: false
      MQTT_PROTOCOL:
        required: false
      MQTT_HOST:
        required: false
      MQTT_PORT:
        required: false
      MQTT_USERNAME:
        required: false
      MQTT_PASSWORD:
        required: false
      MQTT_CA_CERT:
        required: false
      AWS_OIDC_ROLE_ARN:
        required: true

permissions:
  actions: read
  id-token: write
  contents: read

jobs:
  report-start:
    name: "[android] report-start"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    outputs:
      comment-ids: ${{ steps.report.outputs.comment-ids }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 6.0.2
        with:
          sparse-checkout: .github/actions/sdk-e2e-report-start
          sparse-checkout-cone-mode: false

      - name: Post running comment
        id: report
        uses: ./.github/actions/sdk-e2e-report-start
        with:
          family: android
          platforms: '["android"]'
          suite: ${{ inputs.suite }}
          filter: ${{ inputs.filter }}
          exclude-suite: ${{ inputs.exclude-suite }}

  build:
    name: "[android] build"
    runs-on: qvac-ubuntu2204-x64
    environment: release
    timeout-minutes: 30
    outputs:
      runId: ${{ steps.runid.outputs.runId }}
    env:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
      MQTT_PROTOCOL: ${{ secrets.MQTT_PROTOCOL }}
      MQTT_HOST: ${{ secrets.MQTT_HOST }}
      MQTT_PORT: ${{ secrets.MQTT_PORT }}
      MQTT_USERNAME: ${{ secrets.MQTT_USERNAME }}
      MQTT_PASSWORD: ${{ secrets.MQTT_PASSWORD }}
    steps:
      - name: Manual Workspace Cleanup
        run: rm -rf "$GITHUB_WORKSPACE" && mkdir -p "$GITHUB_WORKSPACE"
        shell: bash
        working-directory: .
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 6.0.2
        with:
          ref: ${{ inputs.test-version || github.event.pull_request.head.sha || github.sha }}
          token: ${{ secrets.PAT_TOKEN }}
          sparse-checkout: ${{ inputs.project-directory }}

      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # 6.3.0
        with:
          node-version: 22

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # 2.2.0
        with:
          bun-version: latest

      - uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # 5.2.0
        with:
          java-version: "17"
          distribution: "temurin"

      - uses: android-actions/setup-android@9fc6c4e9069bf8d3d10b2204b1fb8f6ef7065407 # 3.2.2

      - name: Generate runId
        id: runid
        shell: node {0}
        env:
          EXPECTED_JOB_NAME: "[android] build"
          PR_NUMBER: ${{ github.event.pull_request.number || '' }}
          CHECKOUT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          const fs = require('fs');

          (async () => {
          async function fetchJobs(page) {
            const url =
              `https://api.github.com/repos/${process.env.GITHUB_REPOSITORY}` +
              `/actions/runs/${process.env.GITHUB_RUN_ID}` +
              `/attempts/${process.env.GITHUB_RUN_ATTEMPT}/jobs?per_page=100&page=${page}`;
            const res = await fetch(url, {
              headers: {
                Authorization: `Bearer ${process.env.GITHUB_TOKEN}`,
                Accept: 'application/vnd.github+json',
                'X-GitHub-Api-Version': '2022-11-28',
              },
            });
            if (!res.ok) {
              throw new Error(`Failed to list workflow jobs: ${res.status} ${await res.text()}`);
            }
            return res.json();
          }

          const jobs = [];
          for (let page = 1; ; page++) {
            const data = await fetchJobs(page);
            jobs.push(...(data.jobs || []));
            if (!data.jobs || data.jobs.length < 100) break;
          }

          const matches = jobs.filter(
            (job) => job.name === process.env.EXPECTED_JOB_NAME ||
              job.name.endsWith(` / ${process.env.EXPECTED_JOB_NAME}`),
          );
          const runnerMatches = matches.filter((job) => job.runner_name === process.env.RUNNER_NAME);
          const job = runnerMatches.length === 1 ? runnerMatches[0] : matches[0];
          if (!job) {
            throw new Error(`Could not resolve current job id for ${process.env.EXPECTED_JOB_NAME}`);
          }

          const sha = (process.env.CHECKOUT_SHA || process.env.GITHUB_SHA).substring(0, 7);
          const refPart = process.env.PR_NUMBER ? `pr${process.env.PR_NUMBER}-${sha}` : sha;
          // Base36-encode the numeric job.id so the literal decimal digits
          // no longer appear in the runId. GitHub's secret masker does a
          // substring match against masked secret values; if a short numeric
          // secret (e.g. `secrets.MQTT_PORT`) happens to appear as a
          // substring of the decimal job.id, the masker will both redact
          // the runId in logs and silently drop the job-level output with a
          // "Skip output 'runId' since it may contain secret." warning,
          // breaking every downstream `needs.build.outputs.runId` consumer.
          // Encoding job.id as base36 keeps the value unique and stable
          // while structurally eliminating numeric-substring collisions.
          const jobIdEncoded = BigInt(job.id).toString(36);
          const runId = `android-${refPart}-${jobIdEncoded}`;

          fs.appendFileSync(process.env.GITHUB_OUTPUT, `runId=${runId}\n`, 'utf8');
          console.log(`Generated runId: ${runId}`);
          })().catch((err) => {
            console.error(err);
            process.exit(1);
          });

      - name: Configure npm registries
        shell: node {0}
        run: |
          const fs = require('fs');
          const path = require('path');

          if (!process.env.NPM_TOKEN) { console.log('NPM_TOKEN is empty'); process.exit(1); }
          if (!process.env.PAT_TOKEN) { console.log('PAT_TOKEN is empty'); process.exit(1); }

          const lines = [
            'registry=https://registry.npmjs.org/',
            '@qvac:registry=https://registry.npmjs.org/',
            '@tetherto:registry=https://npm.pkg.github.com/',
            `//registry.npmjs.org/:_authToken=${process.env.NPM_TOKEN}`,
            `//npm.pkg.github.com/:_authToken=${process.env.PAT_TOKEN}`
          ];
          const npmrc = lines.join('\n');
          const target = path.join('${{ inputs.project-directory }}', '.npmrc');
          fs.writeFileSync(target, npmrc, 'utf8');
          console.log(`.npmrc configured at ${target}`);

      - name: Setup MQTT CA certificate
        shell: node {0}
        env:
          CA_CERT: ${{ secrets.MQTT_CA_CERT }}
        run: |
          const fs = require('fs');
          const path = require('path');
          const os = require('os');

          const caCert = (process.env.CA_CERT || '').trim();
          if (caCert) {
            const certsDir = path.join(os.tmpdir(), 'mqtt-certs');
            fs.mkdirSync(certsDir, { recursive: true });
            const caPath = path.join(certsDir, 'ca.crt');
            fs.writeFileSync(caPath, caCert, 'utf8');
            fs.appendFileSync(process.env.GITHUB_ENV, `MQTT_CA_PATH=${caPath}\n`, 'utf8');
            console.log('CA certificate written');
          }

      - name: Install dependencies (project)
        working-directory: ${{ inputs.project-directory }}
        run: bun install

      - name: Build project
        working-directory: ${{ inputs.project-directory }}
        shell: node {0}
        run: |
          const { execSync } = require('child_process');
          const fs = require('fs');

          const packageJson = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          if (packageJson.scripts && packageJson.scripts.build) {
            console.log('Building project...');
            execSync('bun run build', { stdio: 'inherit' });
          } else {
            console.log('No build script found in package.json - skipping');
          }

      - name: Copy .npmrc to consumer directory
        shell: node {0}
        run: |
          const fs = require('fs');
          const path = require('path');
          const projectDir = '${{ inputs.project-directory }}';
          const consumerDir = '${{ inputs.working-directory }}';
          if (consumerDir !== projectDir) {
            const source = path.join(projectDir, '.npmrc');
            const npmrcContent = fs.readFileSync(source, 'utf8');
            fs.writeFileSync(path.join(consumerDir, '.npmrc'), npmrcContent, 'utf8');
            console.log('.npmrc copied to consumer directory');
          }

      - name: Install dependencies (consumer)
        if: inputs.working-directory != inputs.project-directory
        working-directory: ${{ inputs.working-directory }}
        run: npm install --install-links

      - name: Build consumer
        if: inputs.working-directory != inputs.project-directory
        working-directory: ${{ inputs.working-directory }}
        run: bun run build

      # ggml co-load smoke (run from PR code): overlay THIS PR's freshly-built
      # android-arm64 prebuild onto the installed @qvac/<addon> package so the
      # bundled consumer co-loads the PR's addon, not the npm baseline. No-op for
      # normal SDK e2e runs (overlay-prebuild-addon empty).
      - name: Download this PR's freshly-built prebuilds (co-load overlay)
        if: ${{ inputs.overlay-prebuild-addon != '' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # 8.0.1
        with:
          name: prebuilds
          path: ${{ runner.temp }}/pr-prebuilds

      - name: Overlay PR android-arm64 prebuild (co-load smoke)
        if: ${{ inputs.overlay-prebuild-addon != '' }}
        shell: bash
        env:
          ADDON: ${{ inputs.overlay-prebuild-addon }}
          SRC: ${{ runner.temp }}/pr-prebuilds
          PROJECT_DIR: ${{ inputs.project-directory }}
          CONSUMER_DIR: ${{ inputs.working-directory }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC/android-arm64" ]; then
            echo "::error::PR prebuilds artifact has no android-arm64/ for $ADDON"
            ls -R "$SRC" || true
            exit 1
          fi
          # The addon can be hoisted into either the consumer or the project
          # node_modules; overlay every install so the bundler cannot pick up a
          # stale baseline copy.
          mapfile -t PKG_DIRS < <(find "$CONSUMER_DIR" "$PROJECT_DIR" -type d -path "*/node_modules/@qvac/$ADDON" 2>/dev/null || true)
          if [ "${#PKG_DIRS[@]}" -eq 0 ]; then
            echo "::error::no installed @qvac/$ADDON found under $CONSUMER_DIR or $PROJECT_DIR to overlay"
            exit 1
          fi
          for pkg in "${PKG_DIRS[@]}"; do
            dest="$pkg/prebuilds/android-arm64"
            mkdir -p "$dest"
            cp -r "$SRC/android-arm64/." "$dest/"
            # Published @qvac packages use the qvac_ prefix; a freshly-built
            # prebuild may still carry the tetherto_ build prefix.
            find "$pkg/prebuilds" -depth -name 'tetherto_*' | while read -r f; do
              mv "$f" "$(dirname "$f")/$(basename "$f" | sed 's/^tetherto_/qvac_/')"
            done
            echo "Overlaid PR android-arm64 prebuild for $ADDON into $dest"
          done

      # ggml co-load smoke: restrict the mobile bundle to a subset of plugins so
      # the consumer co-loads exactly those addons. No-op for normal SDK e2e
      # runs (plugins input empty -> default full plugin bundle).
      - name: Write plugin-subset config (co-load smoke)
        if: ${{ inputs.plugins != '' }}
        working-directory: ${{ inputs.working-directory }}
        shell: bash
        env:
          PLUGINS: ${{ inputs.plugins }}
        run: |
          set -euo pipefail
          node -e 'const fs=require("fs");const p=(process.env.PLUGINS||"").split(",").map(s=>s.trim()).filter(Boolean);fs.writeFileSync("qvac.config.json",JSON.stringify({plugins:p},null,2)+"\n");console.log("[co-load] wrote qvac.config.json plugins:",p.join(", "))'

      - name: Build mobile consumer APK
        working-directory: ${{ inputs.working-directory }}
        env:
          NPM_CONFIG_USERCONFIG: ${{ github.workspace }}/${{ inputs.working-directory }}/.npmrc
        run: |
          echo "Building Android consumer APK..."
          echo "   Run ID: ${{ steps.runid.outputs.runId }}"
          echo "   MQTT Host: $MQTT_HOST"

          npx qvac-test build:consumer:android \
            --runId "${{ steps.runid.outputs.runId }}" \
            --config .

          APK_PATH="build/consumers/android/consumer.apk"
          if [ -f "$APK_PATH" ]; then
            echo "APK built successfully"
            ls -lh "$APK_PATH"
          else
            echo "APK not found at $APK_PATH"
            find build -name "*.apk" 2>/dev/null || echo "No APK files found"
            exit 1
          fi

      - name: Upload APK artifact
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # 7.0.0
        with:
          name: consumer-apk-android
          path: ${{ inputs.working-directory }}/build/consumers/android/consumer.apk
          retention-days: 7

  select-pool:
    name: "[android] select-pool"
    runs-on: ubuntu-latest
    environment: release
    permissions:
      id-token: write
      contents: read
    outputs:
      pool: ${{ steps.select.outputs.pool }}
      name: ${{ steps.select.outputs.name }}
      availability: ${{ steps.select.outputs.availability }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 6.0.2
        with:
          sparse-checkout: .github/actions/sdk-device-farm-select-pool
          sparse-checkout-cone-mode: false

      # Only random selection queries Device Farm (GetDevicePool + ListDevices);
      # an explicit pool name needs no AWS access.
      - name: Configure AWS credentials
        if: ${{ inputs.device-pool-name == '' }}
        uses: aws-actions/configure-aws-credentials@8df5847569e6427dd6c4fb1cf565c83acfa8afa7 # 6.0.0
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: us-west-2

      - name: Select device pool
        id: select
        uses: ./.github/actions/sdk-device-farm-select-pool
        with:
          # Normalise to strict JSON: the device-pools variable may use lenient
          # (unquoted-key) JSON that GitHub's fromJSON tolerates but jq does not.
          pools: ${{ toJSON(fromJSON(inputs.device-pools)) }}
          pool-name: ${{ inputs.device-pool-name }}

  device-farm:
    name: "[android] device-farm"
    needs: [build, select-pool]
    runs-on: ubuntu-latest
    environment: release
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        device: ${{ fromJson(needs.select-pool.outputs.pool) }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 6.0.2
        with:
          ref: ${{ inputs.test-version || github.event.pull_request.head.sha || github.sha }}
          sparse-checkout: ${{ inputs.working-directory }}/device-farm

      - name: Download APK artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # 8.0.1
        with:
          name: consumer-apk-android
          path: ./apk

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@8df5847569e6427dd6c4fb1cf565c83acfa8afa7 # 6.0.0
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: us-west-2

      - name: Upload APK to Device Farm
        id: upload-app
        run: |
          echo "Uploading APK to Device Farm for ${{ matrix.device.name }}..."

          UPLOAD_RESPONSE=$(aws devicefarm create-upload \
            --project-arn "${{ inputs.device-farm-project-arn }}" \
            --name "consumer-${{ needs.build.outputs.runId }}-${{ matrix.device.name }}.apk" \
            --type "ANDROID_APP" \
            --output json)

          APP_UPLOAD_URL=$(echo $UPLOAD_RESPONSE | jq -r '.upload.url')
          APP_UPLOAD_ARN=$(echo $UPLOAD_RESPONSE | jq -r '.upload.arn')

          echo "app-arn=$APP_UPLOAD_ARN" >> $GITHUB_OUTPUT

          curl -T "./apk/consumer.apk" "$APP_UPLOAD_URL"

          echo "Waiting for APK to be processed..."
          for i in {1..30}; do
            STATUS=$(aws devicefarm get-upload --arn "$APP_UPLOAD_ARN" --query "upload.status" --output text)
            echo "Status (attempt $i/30): $STATUS"

            if [ "$STATUS" = "SUCCEEDED" ]; then
              echo "APK upload successful"
              break
            fi

            if [ "$STATUS" = "FAILED" ]; then
              echo "APK upload failed"
              aws devicefarm get-upload --arn "$APP_UPLOAD_ARN"
              exit 1
            fi

            sleep 10
          done

      - name: Create and upload dummy test package
        id: upload-test-pkg
        run: |
          mkdir -p /tmp/dummy-test-pkg
          echo '{"name":"qvac-test-stub","version":"1.0.0"}' > /tmp/dummy-test-pkg/package.json
          cp "${{ inputs.working-directory }}/device-farm/gen-junit.py" /tmp/dummy-test-pkg/
          (cd /tmp/dummy-test-pkg && zip -r /tmp/dummy-test-pkg.zip .)

          PKG_RESPONSE=$(aws devicefarm create-upload \
            --project-arn "${{ inputs.device-farm-project-arn }}" \
            --name "qvac-test-stub.zip" \
            --type "APPIUM_NODE_TEST_PACKAGE" \
            --output json)

          PKG_URL=$(echo $PKG_RESPONSE | jq -r '.upload.url')
          PKG_ARN=$(echo $PKG_RESPONSE | jq -r '.upload.arn')

          echo "pkg-arn=$PKG_ARN" >> $GITHUB_OUTPUT

          curl -T /tmp/dummy-test-pkg.zip "$PKG_URL"

          for i in {1..15}; do
            STATUS=$(aws devicefarm get-upload --arn "$PKG_ARN" --query "upload.status" --output text)
            if [ "$STATUS" = "SUCCEEDED" ]; then
              echo "Test package upload successful"
              break
            fi
            if [ "$STATUS" = "FAILED" ]; then
              echo "Test package upload failed"
              aws devicefarm get-upload --arn "$PKG_ARN"
              exit 1
            fi
            sleep 5
          done

      - name: Upload test spec to Device Farm
        id: upload-spec
        run: |
          SPEC_RESPONSE=$(aws devicefarm create-upload \
            --project-arn "${{ inputs.device-farm-project-arn }}" \
            --name "qvac-test-spec-${{ needs.build.outputs.runId }}.yml" \
            --type "APPIUM_NODE_TEST_SPEC" \
            --output json)

          SPEC_URL=$(echo $SPEC_RESPONSE | jq -r '.upload.url')
          SPEC_ARN=$(echo $SPEC_RESPONSE | jq -r '.upload.arn')

          echo "spec-arn=$SPEC_ARN" >> $GITHUB_OUTPUT

          TIMEOUT_SECONDS=$(( ${{ inputs.device-farm-timeout || 90 }} * 60 ))
          sed -i "s/DEVICEFARM_TIMEOUT:-5400/DEVICEFARM_TIMEOUT:-${TIMEOUT_SECONDS}/" "${{ inputs.working-directory }}/device-farm/test-spec-android.yml"
          curl -T "${{ inputs.working-directory }}/device-farm/test-spec-android.yml" "$SPEC_URL"

          echo "Waiting for test spec to be processed..."
          for i in {1..15}; do
            STATUS=$(aws devicefarm get-upload --arn "$SPEC_ARN" --query "upload.status" --output text)
            echo "Status (attempt $i/15): $STATUS"

            if [ "$STATUS" = "SUCCEEDED" ]; then
              echo "Test spec upload successful"
              break
            fi

            if [ "$STATUS" = "FAILED" ]; then
              echo "Test spec upload failed"
              aws devicefarm get-upload --arn "$SPEC_ARN"
              exit 1
            fi

            sleep 5
          done

      - name: Schedule Device Farm run
        id: schedule
        run: |
          echo "Scheduling Device Farm run on ${{ matrix.device.name }}..."

          RUN_NAME="mobile-${{ matrix.device.name }}-${{ github.run_number }}-${{ github.run_attempt }}"

          RUN_ARN=$(aws devicefarm schedule-run \
            --project-arn "${{ inputs.device-farm-project-arn }}" \
            --device-pool-arn "${{ matrix.device.arn }}" \
            --app-arn "${{ steps.upload-app.outputs.app-arn }}" \
            --name "$RUN_NAME" \
            --test type=APPIUM_NODE,testPackageArn=${{ steps.upload-test-pkg.outputs.pkg-arn }},testSpecArn=${{ steps.upload-spec.outputs.spec-arn }} \
            --execution-configuration jobTimeoutMinutes=${{ inputs.device-farm-timeout }} \
            --query 'run.arn' --output text)

          echo "run-arn=$RUN_ARN" >> $GITHUB_OUTPUT
          echo "Device Farm run scheduled: $RUN_ARN"

          PROJECT_ID=$(echo "${{ inputs.device-farm-project-arn }}" | sed -n 's/.*:project:\(.*\)/\1/p')
          RUN_ID=$(echo "$RUN_ARN" | sed -n 's/.*:run:[^/]*\/\(.*\)/\1/p')
          DF_URL="https://us-west-2.console.aws.amazon.com/devicefarm/home#/mobile/projects/${PROJECT_ID}/runs/${RUN_ID}"
          echo "Device Farm Console (${{ matrix.device.name }}): $DF_URL"

          echo "### Device Farm: ${{ matrix.device.name }}" >> $GITHUB_STEP_SUMMARY
          echo "[Open Device Farm Console]($DF_URL)" >> $GITHUB_STEP_SUMMARY

      - name: Save run ARN
        id: save-arn
        run: |
          SAFE_NAME=$(echo "${{ matrix.device.name }}" | tr ' /\\:*?"<>|' '_')
          echo "${{ steps.schedule.outputs.run-arn }}" > "${SAFE_NAME}.txt"
          echo "safe-name=${SAFE_NAME}" >> $GITHUB_OUTPUT

      - name: Upload run ARN
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # 7.0.0
        with:
          name: device-farm-arn-${{ needs.build.outputs.runId }}-${{ steps.save-arn.outputs.safe-name }}
          path: ${{ steps.save-arn.outputs.safe-name }}.txt
          retention-days: 1

  wait-device-farm-running:
    name: "[android] wait-device-farm-running"
    needs: [build, device-farm]
    runs-on: ubuntu-latest
    environment: release
    timeout-minutes: ${{ inputs.device-farm-start-timeout }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 6.0.2
        with:
          sparse-checkout: .github/actions/sdk-device-farm-wait-for-running
          sparse-checkout-cone-mode: false

      - name: Download run ARNs
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # 8.0.1
        with:
          pattern: device-farm-arn-${{ needs.build.outputs.runId }}-*
          path: ./run-arns
          merge-multiple: true

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@8df5847569e6427dd6c4fb1cf565c83acfa8afa7 # 6.0.0
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: us-west-2

      - name: Wait for Device Farm runs to enter RUNNING
        uses: ./.github/actions/sdk-device-farm-wait-for-running
        with:
          platform: android
          run-arns-directory: ./run-arns
          start-timeout-minutes: ${{ inputs.device-farm-start-timeout }}

  run-producer:
    name: "[android] run-producer"
    needs: [build, wait-device-farm-running]
    runs-on: ubuntu-latest
    environment: release
    timeout-minutes: 90
    env:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
      MQTT_PROTOCOL: ${{ secrets.MQTT_PROTOCOL }}
      MQTT_HOST: ${{ secrets.MQTT_HOST }}
      MQTT_PORT: ${{ secrets.MQTT_PORT }}
      MQTT_USERNAME: ${{ secrets.MQTT_USERNAME }}
      MQTT_PASSWORD: ${{ secrets.MQTT_PASSWORD }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 6.0.2
        with:
          ref: ${{ inputs.test-version || github.event.pull_request.head.sha || github.sha }}
          token: ${{ secrets.PAT_TOKEN }}
          sparse-checkout: |
            ${{ inputs.project-directory }}
            .github/actions/sdk-e2e-fail-on-failures

      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # 6.3.0
        with:
          node-version: 22

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # 2.2.0
        with:
          bun-version: latest

      - name: Configure npm registries
        shell: node {0}
        run: |
          const fs = require('fs');
          const path = require('path');

          const lines = [
            'registry=https://registry.npmjs.org/',
            '@qvac:registry=https://registry.npmjs.org/',
            '@tetherto:registry=https://npm.pkg.github.com/',
            `//registry.npmjs.org/:_authToken=${process.env.NPM_TOKEN}`,
            `//npm.pkg.github.com/:_authToken=${process.env.PAT_TOKEN}`
          ];
          const npmrc = lines.join('\n');
          const target = path.join('${{ inputs.project-directory }}', '.npmrc');
          fs.writeFileSync(target, npmrc, 'utf8');

      - name: Setup MQTT CA certificate
        shell: node {0}
        env:
          CA_CERT: ${{ secrets.MQTT_CA_CERT }}
        run: |
          const fs = require('fs');
          const path = require('path');
          const os = require('os');

          const caCert = (process.env.CA_CERT || '').trim();
          if (caCert) {
            const certsDir = path.join(os.tmpdir(), 'mqtt-certs');
            fs.mkdirSync(certsDir, { recursive: true });
            const caPath = path.join(certsDir, 'ca.crt');
            fs.writeFileSync(caPath, caCert, 'utf8');
            fs.appendFileSync(process.env.GITHUB_ENV, `MQTT_CA_PATH=${caPath}\n`, 'utf8');
            console.log('CA certificate written');
          }

      - name: Copy .npmrc to consumer directory
        shell: node {0}
        run: |
          const fs = require('fs');
          const path = require('path');
          const projectDir = '${{ inputs.project-directory }}';
          const consumerDir = '${{ inputs.working-directory }}';
          if (consumerDir !== projectDir) {
            const source = path.join(projectDir, '.npmrc');
            const npmrcContent = fs.readFileSync(source, 'utf8');
            fs.writeFileSync(path.join(consumerDir, '.npmrc'), npmrcContent, 'utf8');
          }

      - name: Install dependencies (project)
        working-directory: ${{ inputs.project-directory }}
        run: bun install

      - name: Build project
        working-directory: ${{ inputs.project-directory }}
        shell: node {0}
        run: |
          const { execSync } = require('child_process');
          const fs = require('fs');

          const packageJson = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          if (packageJson.scripts && packageJson.scripts.build) {
            execSync('bun run build', { stdio: 'inherit' });
          }

      - name: Install dependencies (consumer)
        if: inputs.working-directory != inputs.project-directory
        working-directory: ${{ inputs.working-directory }}
        run: npm install --install-links

      - name: Build consumer
        if: inputs.working-directory != inputs.project-directory
        working-directory: ${{ inputs.working-directory }}
        run: bun run build

      - name: Run producer
        working-directory: ${{ inputs.working-directory }}
        run: |
          echo "Starting producer..."
          echo "   Run ID: ${{ needs.build.outputs.runId }}"
          echo "   Consumer timeout: ${{ inputs.consumer-timeout }}s"
          echo "   Filter: ${{ inputs.filter || '(none)' }}"
          echo "   Suite: ${{ inputs.suite || '(all)' }}"
          echo "   Exclude suite: ${{ inputs.exclude-suite || '(none)' }}"

          FILTER_ARG=""
          if [ -n "${{ inputs.filter }}" ]; then
            FILTER_ARG="--filter=${{ inputs.filter }}"
          fi

          SUITE_ARG=""
          if [ -n "${{ inputs.suite }}" ]; then
            SUITE_ARG="--suite=${{ inputs.suite }}"
          fi

          EXCLUDE_SUITE_ARG=""
          if [ -n "${{ inputs.exclude-suite }}" ]; then
            EXCLUDE_SUITE_ARG="--exclude-suite=${{ inputs.exclude-suite }}"
          fi

          npx qvac-test run:producer \
            --runId "${{ needs.build.outputs.runId }}" \
            --consumer-timeout "${{ inputs.consumer-timeout }}" \
            --config . \
            --report-dir=./reports \
            $FILTER_ARG \
            $SUITE_ARG \
            $EXCLUDE_SUITE_ARG

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # 7.0.0
        with:
          name: results-android
          path: ${{ inputs.working-directory }}/reports/
          retention-days: 30

      - name: Download baseline (best-effort)
        if: always() && github.event.pull_request.base.ref != ''
        uses: dawidd6/action-download-artifact@20319c5641d495c8a52e688b7dc5fada6c3a9fbc # v8
        continue-on-error: true
        with:
          workflow: test-android-sdk.yml
          branch: ${{ github.event.pull_request.base.ref }}
          name: baseline-android
          path: ${{ inputs.working-directory }}/baseline
          github_token: ${{ github.token }}
          if_no_artifact_found: warn

      - name: Compare vs baseline
        if: always()
        working-directory: ${{ inputs.working-directory }}
        shell: node {0}
        run: |
          const fs = require('fs');
          const path = require('path');
          const { execSync } = require('child_process');

          if (!fs.existsSync('baseline')) {
            console.log('No baseline directory - skipping compare.');
            process.exit(0);
          }
          const baselineFiles = fs.readdirSync('baseline').filter((f) => f.endsWith('.json'));
          if (baselineFiles.length === 0) {
            console.log('Baseline directory contains no json files - skipping compare.');
            process.exit(0);
          }
          if (!fs.existsSync('reports')) {
            console.log('No reports directory - skipping compare.');
            process.exit(0);
          }
          const currentFiles = fs.readdirSync('reports')
            .filter((f) => f.startsWith('results-') && f.endsWith('.json'));
          if (currentFiles.length === 0) {
            console.log('No current results json - skipping compare.');
            process.exit(0);
          }

          const baseline = path.join('baseline', baselineFiles[0]);
          const current = path.join('reports', currentFiles[0]);
          execSync(
            `npx qvac-test report:compare --baseline "${baseline}" --current "${current}" --output "comparison-android.json"`,
            { stdio: 'inherit' }
          );

      - name: Format comparison
        if: always()
        working-directory: ${{ inputs.working-directory }}
        shell: node {0}
        run: |
          const fs = require('fs');
          const { execSync } = require('child_process');

          const input = 'comparison-android.json';
          if (!fs.existsSync(input)) {
            console.log('No comparison json - skipping format.');
            process.exit(0);
          }
          execSync(
            `npx qvac-test report:format --input "${input}" --format markdown --output "section-android.md"`,
            { stdio: 'inherit' }
          );

      - name: Upload report section
        if: always()
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # 7.0.0
        with:
          name: report-section-android
          path: |
            ${{ inputs.working-directory }}/section-*.md
            ${{ inputs.working-directory }}/comparison-*.json
          retention-days: 7
          if-no-files-found: ignore

      - name: Upload baseline (main/develop only)
        if: always() && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # 7.0.0
        with:
          name: baseline-android
          path: ${{ inputs.working-directory }}/reports/results-*.json
          retention-days: 90
          overwrite: true

      - name: Fail on e2e result failures
        if: success() || failure()
        uses: ./.github/actions/sdk-e2e-fail-on-failures
        with:
          report-dir: ${{ inputs.working-directory }}/reports

      - name: Download run ARNs for cancellation cleanup
        if: cancelled()
        continue-on-error: true
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # 8.0.1
        with:
          pattern: device-farm-arn-${{ needs.build.outputs.runId }}-*
          path: ./run-arns
          merge-multiple: true

      - name: Configure AWS credentials for cancellation cleanup
        if: cancelled()
        uses: aws-actions/configure-aws-credentials@8df5847569e6427dd6c4fb1cf565c83acfa8afa7 # 6.0.0
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: us-west-2

      - name: Stop Device Farm runs on cancellation
        if: cancelled()
        run: |
          for f in ./run-arns/*.txt; do
            [ -f "$f" ] || continue
            arn=$(cat "$f")
            echo "Stopping Device Farm run: $arn"
            aws devicefarm stop-run --arn "$arn" || echo "stop-run failed for $arn"
          done

  cleanup-device-farm:
    name: "[android] cleanup-device-farm"
    needs: [build, device-farm, wait-device-farm-running, run-producer]
    if: always()
    runs-on: ubuntu-latest
    environment: release
    timeout-minutes: 35
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@8df5847569e6427dd6c4fb1cf565c83acfa8afa7 # 6.0.0
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: us-west-2

      - name: Download run ARNs
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # 8.0.1
        with:
          pattern: device-farm-arn-${{ needs.build.outputs.runId }}-*
          path: ./run-arns
          merge-multiple: true

      - name: Stop Device Farm runs and download artifacts
        run: |
          echo "Cleaning up Device Farm runs for runId: ${{ needs.build.outputs.runId }}"
          mkdir -p ./device-farm-logs

          for arn_file in run-arns/*.txt; do
            if [ -f "$arn_file" ]; then
              RUN_ARN=$(cat "$arn_file")
              DEVICE_NAME=$(basename "$arn_file" .txt)

              STATUS=$(aws devicefarm get-run --arn "$RUN_ARN" --query 'run.status' --output text 2>/dev/null || echo "UNKNOWN")
              echo "Device $DEVICE_NAME: status=$STATUS"

              if [ "$STATUS" = "RUNNING" ] || [ "$STATUS" = "PENDING" ]; then
                echo "Stopping run: $RUN_ARN"
                aws devicefarm stop-run --arn "$RUN_ARN" || true
              fi

              echo "Waiting for run to finish: $DEVICE_NAME (up to 30 min)..."
              for attempt in $(seq 1 120); do
                STATUS=$(aws devicefarm get-run --arn "$RUN_ARN" --query 'run.status' --output text 2>/dev/null || echo "UNKNOWN")
                if [ "$STATUS" = "COMPLETED" ] || [ "$STATUS" = "STOPPED" ] || [ "$STATUS" = "ERRORED" ]; then
                  echo "Run $DEVICE_NAME finished with status: $STATUS"
                  break
                fi
                if [ $((attempt % 6)) -eq 0 ]; then
                  echo "Run $DEVICE_NAME still $STATUS (${attempt}/120, $((attempt * 15))s elapsed)..."
                fi
                sleep 15
              done

              DEVICE_DIR="./device-farm-logs/$DEVICE_NAME"
              mkdir -p "$DEVICE_DIR"

              echo "Downloading Customer Artifacts for $DEVICE_NAME..."
              JOBS=$(aws devicefarm list-jobs --arn "$RUN_ARN" --query 'jobs[*].arn' --output text 2>/dev/null || echo "")
              for JOB_ARN in $JOBS; do
                aws devicefarm list-artifacts \
                  --arn "$JOB_ARN" \
                  --type FILE \
                  --query "artifacts[?contains(name,'Customer')].[name,extension,url]" \
                  --output text 2>/dev/null \
                | while IFS=$'\t' read -r NAME EXT URL; do
                    if [ -z "$URL" ] || [ "$URL" = "None" ]; then
                      continue
                    fi
                    SAFE_NAME=$(echo "$NAME" | tr ' /' '__')
                    echo "  Downloading ${SAFE_NAME}.${EXT}"
                    curl -sS -o "$DEVICE_DIR/${SAFE_NAME}.${EXT}" "$URL" || echo "  Failed: ${SAFE_NAME}.${EXT}"
                  done
              done

              echo "Artifacts for $DEVICE_NAME:"
              ls -lh "$DEVICE_DIR" 2>/dev/null || echo "  (empty)"
            fi
          done

      - name: Upload Device Farm logs
        if: always()
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # 7.0.0
        with:
          name: device-farm-logs-android-${{ needs.build.outputs.runId }}
          path: ./device-farm-logs/
          retention-days: 30

  report-finalize:
    name: "[android] report-finalize"
    needs: [report-start, run-producer, select-pool]
    if: always()
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 6.0.2
        with:
          sparse-checkout: .github/actions/sdk-e2e-report-finalize
          sparse-checkout-cone-mode: false

      - uses: ./.github/actions/sdk-e2e-report-finalize
        with:
          family: android
          platforms: '["android"]'
          comment-ids: ${{ needs.report-start.outputs.comment-ids }}
          suite: ${{ inputs.suite }}
          filter: ${{ inputs.filter }}
          exclude-suite: ${{ inputs.exclude-suite }}
          device-pool-name: ${{ needs.select-pool.outputs.name }}
          device-pool-availability: ${{ needs.select-pool.outputs.availability }}

  report-artifacts-refresh:
    name: "[android] report-artifacts-refresh"
    needs: [report-start, run-producer, cleanup-device-farm, select-pool]
    if: always()
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 6.0.2
        with:
          sparse-checkout: .github/actions/sdk-e2e-report-finalize
          sparse-checkout-cone-mode: false

      - uses: ./.github/actions/sdk-e2e-report-finalize
        with:
          family: android
          platforms: '["android"]'
          comment-ids: ${{ needs.report-start.outputs.comment-ids }}
          suite: ${{ inputs.suite }}
          filter: ${{ inputs.filter }}
          exclude-suite: ${{ inputs.exclude-suite }}
          device-pool-name: ${{ needs.select-pool.outputs.name }}
          device-pool-availability: ${{ needs.select-pool.outputs.availability }}
          update-summary: "false"
